#ifndef APPLICATIONICON_H
#define APPLICATIONICON_H

#include <QObject>

class ApplicationIcon : public QObject
{
    Q_OBJECT
public:
    explicit ApplicationIcon(QObject *parent = 0);

    QString findIcon(QString icon, )

signals:

public slots:
};

#endif // APPLICATIONICON_H
